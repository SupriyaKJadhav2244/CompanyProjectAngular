import { DisplayStudentsProfile } from './display-students-profile';

describe('DisplayStudentsProfile', () => {
  it('should create an instance', () => {
    expect(new DisplayStudentsProfile()).toBeTruthy();
  });
});
